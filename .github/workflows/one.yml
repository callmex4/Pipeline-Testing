name: approval
on: workflow_dispatch
jobs:
   build:
    runs-on: ubuntu-latest
    steps:
     - name: Build an App
       run: echo "Bulding"
       
   test:
    runs-on: ubuntu-latest
    needs: build
    steps:
     - name: Test an app
       run: echo "testing"
       
   deploy:
     runs-on: ubuntu-latest
     needs: test
     steps: 
     - name: Deploy the app
       run: echo "Deployment"
  
